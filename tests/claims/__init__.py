"""Claims suite — the public promises, measured on planted synthetic worlds.

Separate from `tests/conformance.py` (which grades the build against SPEC.md)
and `tests/unit/` (which grades the pieces). This suite grades the README.
"""
