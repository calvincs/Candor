"""CANDOR — Calibrated Agent Memory Substrate (spec v0.2).

Deliberately empty: importing `candor` must not pull in either the trusted core
or the untrusted periphery, so the import-graph firewall of I2 stays checkable.
"""

__version__ = "0.2.0"
